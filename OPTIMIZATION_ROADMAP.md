# Trace UI 功能优化路线图

## 1. 目标

本轮优化围绕两条主线展开：

1. 增加“已知摘要匹配”能力，让用户输入 MD5、SHA-1、SHA-256 等摘要后，可以在 Trace 中已经出现的字符串和内存数据里寻找候选原文、摘要输出位置及其数据来源。
2. 降低污点分析使用门槛，让不了解寄存器、内存地址和控制依赖的用户也能通过上下文操作完成常见分析。

同时处理会直接影响上述功能正确性和稳定性的基础问题，包括多字节内存污点、任务取消、缓存写入安全和测试覆盖。

## 2. 产品边界

- 已知摘要匹配不是哈希逆向或暴力破解。
- 系统只验证 Trace 中实际出现的字符串、内存缓冲区和其他明确候选数据。
- 默认只执行本地分析，不上传 Trace、摘要或候选字符串。
- 默认采用严格字节匹配，不隐式执行 trim、大小写转换、拼盐等可能制造误报的操作。
- 非严格变换必须由用户显式启用，并在结果中展示具体变换方式。

## 3. 当前能力与已确认问题

### 3.1 可复用能力

- [x] 已有字符串索引，记录字符串内容、地址、出现序号、编码和 XRef 数量。
- [x] 已有内存访问索引，记录地址、值、访问宽度、读写类型和执行序号。
- [x] 已有寄存器和内存反向切片能力。
- [x] 已有依赖 DAG 和表达式树，可用于解释关键数据链路。
- [x] 已有 Crypto 面板和磁盘缓存机制，可承载新的摘要匹配入口和结果。
- [x] 已有 Tauri IPC、MCP 和多会话架构。

### 3.2 已确认问题

- [x] **P0：内存污点范围未生效。** 已实现完整范围解析、逐字节最近定义、去重起点和部分缺失警告。
- [x] **P0：索引构建取消接入扫描器。** 单线程、并行 chunk、merge、状态提交和缓存写入均检查取消状态。
- [x] **P0：缓存写入改为临时文件原子替换。** 同目录写入、flush/sync 后再跨平台替换最终文件。
- [ ] **P1：Crypto Scan 只匹配算法魔数。** 当前无法搜索用户提供的摘要结果，也不会按函数聚合算法证据。
- [ ] **P1：字符串索引主要保存解码后的内容。** 对结尾零字节、UTF-16 和二进制输入无法始终还原精确原始字节。
- [ ] **P1：污点配置默认展示高级概念。** 用户必须理解寄存器、内存地址、行范围和控制依赖。
- [ ] **P1：前端没有自动化测试。** 复杂交互集中在 `App.tsx` 和 `TraceTable.tsx`。

## 4. 优先级说明

- **P0**：正确性或数据安全问题，必须先完成。
- **P1**：本轮核心用户功能。
- **P2**：增强分析质量和解释能力。
- **P3**：长期能力和体验改进。

## 5. 阶段一：多字节内存污点修正

目标：让 16 字节 MD5、20 字节 SHA-1、32 字节 SHA-256 以及普通缓冲区可以作为完整污点源。

### 5.1 后端任务

- [x] 将 `reg:NAME@SEQ` 和 `mem:ADDR:SIZE@SEQ` 解析为明确的内部类型，避免后续继续用字符串拆分承载语义。
- [x] 对外暂时保留现有 `from_specs` 格式，保证 GUI、MCP 和已保存会话兼容。
- [x] 实现 `resolve_mem_range_defs(addr, size, seq)`。
- [x] 为范围内每个字节找到指定序号之前最近的写入定义。
- [x] 合并由同一条 STORE 指令定义的多个字节，避免重复 BFS 起点。
- [x] 正确保留 LDP/STP 等成对指令使用的 dependency tag。
- [x] 对未定义字节返回结构化警告，而不是直接丢弃整个分析。
- [x] 设置合理的最大范围，初始为 4 KiB；超出时要求用户缩小范围。
- [x] 在 SliceOrigin 中保存规范化后的完整源范围，确保重新配置和导出结果一致。
- [x] 依赖图支持多来源和多字节污点的全部起点，不再只展示第一个定义链。

### 5.2 前端任务

- [x] 内存长度至少支持 1、2、4、8、16、20、32、48、64 字节。
- [x] 支持输入 1-4096 字节自定义长度，并进行范围校验。
- [x] 在执行前展示实际目标，例如 `0x1000..0x100f (16 bytes)`。
- [x] 对部分字节没有定义的情况展示清晰提示。

### 5.3 测试与验收

- [x] 单条 16 字节写入可以产生正确的切片起点。
- [x] 由 4 条 4 字节写入组成的 MD5 缓冲区可以合并为多个正确起点。
- [x] 重叠写入时采用目标序号之前每个字节最近的定义。
- [x] 起始地址溢出、零长度和超大长度不会 panic。
- [x] 原有单寄存器、单地址污点测试保持通过。

## 6. 阶段二：已知摘要匹配 MVP

目标：先对已经提取的字符串做可靠、可解释的摘要验证。

### 6.1 首批支持范围

- [x] MD5，16 字节、32 个十六进制字符。
- [x] SHA-1，20 字节、40 个十六进制字符。
- [x] SHA-256，32 字节、64 个十六进制字符。
- [x] 扩展 SHA-384、SHA-512、CRC32，并支持自动长度识别和手动覆盖。

### 6.2 输入规范化

- [x] 接受大小写十六进制。
- [x] 接受空格、冒号、连字符和可选 `0x` 前缀。
- [x] 支持一次输入多个摘要，每行一个。
- [x] 根据字节长度自动建议算法，但允许用户手动覆盖。
- [x] 对长度错误、非十六进制字符和算法冲突提供逐条错误。
- [x] 将摘要解析、算法识别和变换逻辑实现为可独立测试的纯函数。

### 6.3 字符串候选模式

- [x] 默认计算字符串的原始 UTF-8/ASCII 字节摘要。
- [x] 可选 `UTF-8 + NUL`。
- [x] 可选 UTF-16LE 和 `UTF-16LE + NUL`。
- [ ] 可选大小写转换、trim、前缀和后缀，但默认全部关闭。
- [x] 每个结果展示命中的算法、编码和变换方式。
- [x] 结果按“严格原始字节命中”优先排序。

### 6.4 后端设计

- [x] 新增 `query/hash_match.rs`，没有继续扩张 `engine/query.rs`。
- [x] 新增 `HashAlgorithm`、`HashTransform`、`HashMatchRequest`、`HashMatchResult`。
- [x] 初版对 `StringIndex` 流式计算，不提前为所有算法建立常驻索引。
- [ ] 当重复查询性能成为问题时，再增加按算法懒加载的 `digest -> string ids` 派生索引。
- [x] 使用 RustCrypto 实现；MD5、SHA-1、SHA-256 使用对应 crate。
- [ ] 为派生缓存增加独立版本，避免修改 StringRecord 后错误读取旧 bincode 数据。
- [x] Tauri 新增 `match_known_digests` command。
- [x] MCP 新增 `analyze_known_digest` 高层工具，同时完成字符串候选、二进制摘要定位和可选自动污点分析。
- [x] MCP 多字节污点 Schema 对齐 `mem:ADDR:SIZE@LINE`，明确 `@LINE` 为 1-based。
- [x] MCP 污点结果返回部分内存定义警告，不再丢失核心层 `warnings`。
- [x] 核心层增加可序列化依赖摘要，统一输出关键步骤、操作统计、函数、模块、内存输入输出和关键字符串。

### 6.5 前端设计

- [x] Crypto 面板增加 `Detection` 和 `Known Digest` 两个页签。
- [x] 输入区支持粘贴多个摘要。
- [x] 提供算法选择和“高级字节变换”折叠区。
- [x] 结果展示字符串、地址、序号、编码、变换、摘要和 XRef。
- [x] 提供 `Jump to Trace`、`View Memory`、`Show XRefs`、`Trace Input`。
- [x] 空结果时提示可能原因：输入不在 Trace、编码不同、字符串未被提取或输入是二进制缓冲区。

### 6.6 测试与验收

- [x] `MD5("hello")` 能命中字符串 `hello`。
- [x] 大小写和分隔符不同的摘要输入得到相同结果。
- [x] 严格模式不会将 `hello\0` 误报为 `hello`。
- [x] UTF-16LE 仅在用户启用对应变换时匹配。
- [x] 多摘要查询可以区分成功、失败和非法输入。
- [ ] 10 万条字符串候选的查询时间和内存占用可接受，并记录基准。

## 7. 阶段三：内存摘要定位与自动溯源

目标：摘要不是可打印字符串时，仍能从内存写入中找到输出缓冲区并自动追踪输入。

### 7.1 通用内存模式搜索

- [ ] 新增通用 `find_memory_pattern`，支持 Hex、ASCII、整数和摘要字节（摘要字节搜索已完成）。
- [x] 按执行序号重放内存写入，重建稀疏字节内存状态。
- [x] 支持由多条相邻 STORE 组成的连续摘要字节模式。
- [x] 返回地址、首次/最后写入序号及涉及的写入指令序号。
- [ ] 对常见字节序提供显式选项，不进行不可见的自动反转。
- [ ] 为大 Trace 设计锚点或分块策略，避免每次查询全量重放。

### 7.2 自动摘要溯源

- [x] 对每个摘要内存命中，可将整个摘要范围作为污点源。
- [x] 从摘要内存结果自动运行数据依赖反向切片，默认不包含控制依赖。
- [ ] 收集切片中的内存读取、字符串记录、函数范围和外部输入候选。
- [ ] 对可完整重建的候选输入重新计算摘要，区分“已验证匹配”和“相关候选”。
- [ ] 对分块更新、拼接输入、盐值和多缓冲区场景保留“不确定”状态。
- [ ] 允许用户从候选输入继续执行更精确的污点分析。

### 7.3 结果排序

- [ ] 已重新计算且摘要完全一致的候选优先。
- [ ] 与切片关键读取地址重叠的字符串优先。
- [ ] 与输出位于同一函数调用范围的候选优先。
- [ ] 距离输出更近、覆盖字节更多的候选优先。
- [ ] 每个评分项都要能在 UI 中解释，避免黑盒置信度。

### 7.4 测试与验收

- [ ] 摘要由多个 4/8 字节 STORE 写入时仍能找到完整缓冲区。
- [ ] 相同摘要在多个时间点出现时返回不同的地址/序号命中。
- [ ] 后续覆盖内存不会影响早期时间点的匹配。
- [ ] 自动切片可以覆盖摘要全部字节的定义。
- [ ] 无法完整还原输入时不会声称已经找到原文。

## 8. 阶段四：初学者污点分析

目标：常见场景最多通过一次上下文操作和一次确认完成。

### 8.1 两级交互

- [x] 默认展示 `Simple` 模式，保留现有配置作为 `Advanced`。
- [x] Simple 模式只展示目标、分析范围和结果范围。
- [x] Advanced 模式继续提供多个源、寄存器/内存切换、精确行范围和控制依赖。
- [x] Simple 默认“仅数据依赖”，避免控制依赖造成结果爆炸。

### 8.2 上下文入口

- [ ] 寄存器右键：`Trace Origin of Xn`。
- [x] Memory 十六进制视图右键：`Trace Memory Value`，优先使用当前指令的访问地址和宽度。
- [x] 字符串右键：`Trace Creation`，自动使用字符串地址、字节长度和出现序号运行 Focused 污点分析。
- [x] 摘要匹配结果：`Trace Input`，自动选择字符串地址、长度和命中序号。
- [ ] 函数返回位置：`Trace Return Value`，默认选择 X0。
- [ ] 系统自动推断当前行、寄存器、内存地址和访问宽度。

### 8.3 引导式结果

- [x] 增加 `Summary`、`Key Steps`、`All Instructions` 三种视图。
- [x] Summary 展示来源、关键字符串、输入/输出地址、函数、模块和结果数量。
- [x] Key Steps 合并连续 MOV、LOAD、STORE 等搬运链，并保留实际计算指令。
- [x] 复用依赖图节点展示“这个值如何计算得到”，并支持跳转到原始 Trace。
- [x] 点击根节点、关键步骤或完整指令可以跳转到原始 Trace。
- [ ] 支持从简化结果切换到完整高亮/过滤视图。

### 8.4 错误与建议

- [x] 将常见底层错误映射成用户可理解的原因，并保留可折叠的技术细节。
- [x] 未找到定义时建议扩大范围、移动到更晚指令或确认 Trace 是否从计算前开始。
- [ ] 缺少内存注解时直接指出日志格式要求。
- [ ] 结果过多时建议关闭控制依赖或限制到当前函数。
- [x] 结果过少时建议扩大历史范围或切换 Broad scope。

### 8.5 测试与验收

- [ ] 用户从寄存器右键可以直接完成一次反向分析。
- [ ] 用户从字符串结果可以自动选择正确地址和长度。
- [ ] Simple 和 Advanced 对相同配置产生相同的后端请求。
- [ ] 切换会话不会混用污点配置和结果。
- [ ] 失败、空结果和部分结果都有明确 UI 状态。

## 9. 阶段五：算法识别质量提升

- [ ] 将算法魔数命中按函数聚合，而不是只展示独立行。
- [ ] 结合 ARM64 AES、SHA、CRC 指令分类作为证据。
- [ ] 结合循环结构、常量数量、内存读写模式和调用范围计算可解释评分。
- [ ] 显示 `High`、`Medium`、`Low` 置信度及具体证据。
- [ ] 展示疑似算法函数的 X0-X7 入参、返回值和输入/输出缓冲区。
- [ ] 将摘要结果匹配与算法函数识别关联，例如“此 MD5 输出可能由函数 0x... 生成”。
- [ ] 降低单个常量偶然命中造成的误报。

## 10. 后续分析能力

### P2

- [x] 正向污点：回答“这个输入最终影响了什么”，支持寄存器、多字节内存、潜在 Sink、结果上限和 MCP 后台取消。
- [x] 正向依赖索引在 Session 内复用，避免连续 AI 调查重复扫描完整依赖图。
- [x] Source/Sink 基础分类，覆盖内存、栈、JNI、文件、网络、日志、系统调用、函数调用和返回。
- [x] 跨调用句柄来源验证，区分文件、Socket、内存资源并记录创建调用证据。
- [x] Trace Diff：比较不同输入下的动态函数、分支、指令和内存访问站点差异，使用模块相对偏移降低 ASLR 噪声。
- [ ] 关键路径压缩：合并无语义变化的复制链和栈搬运。
- [ ] 函数级输入/输出快照和调用时间线。
- [ ] 通用 Value Search：统一搜索 ASCII、UTF-8、Hex、整数、地址和摘要。
- [x] 一键导出 Markdown/JSON 分析报告，包含分析请求、结果和结构化证据。

### P3

- [x] MCP 分析结果使用 `analysis_id` 保存，支持列表、读取、证据比较和删除；每个 Session 最多保留 100 条。
- [x] 增加 `investigate_crypto_flow` 高层调查工具，组合算法检测、摘要匹配、污点摘要和证据限制。
- [x] 增加 `auto_investigate` 和后台启动工具，组合搜索、Crypto、摘要、正向流、Analysis Compare 与 Trace Diff。
- [x] 可保存和复用的分析配方，支持内置流程、自定义默认参数和 MCP 一键运行。
- [x] 可选的本地 AI/MCP 解释层，但确定性分析结果必须始终可独立使用。
- [ ] 插件化算法签名，允许添加自定义魔数和函数识别规则。
- [ ] 完整中英文界面或统一术语说明。

## 11. 稳定性与工程质量

### 11.1 构建与任务生命周期

- [x] 将 `build_cancel` 传入单线程扫描、并行 chunk 扫描和 merge 阶段。
- [x] 关闭 session 时设置不可复位的 closed 标志和全部取消标志，阻止继续提交会话或写入缓存。
- [ ] 对字符串、密码和摘要扫描统一任务状态与取消机制。
- [ ] 避免同一文件的多个会话并发构建并覆盖同一缓存。

### 11.2 缓存安全

- [x] 所有缓存先写同目录临时文件。
- [x] 写入完成并 flush/sync 后原子重命名；Windows 使用 MoveFileExW replace/write-through。
- [x] SectionReader 校验 section 数量、offset、length、整数溢出、元素长度和类型对齐。
- [x] section/CSR 预验证失败时删除损坏缓存并重新构建，不让坏数据进入运行期视图。
- [ ] 改进源文件指纹，避免只依赖文件长度和前 1 MiB 哈希。
- [ ] 为缓存 schema 增加显式版本和迁移/失效规则。

### 11.3 测试与 CI

- [ ] 提交 `Cargo.lock`，Release 和 CI 使用 `--locked`。
- [ ] 新增普通 push/PR 工作流，而不只在发布 tag 时构建。
- [ ] CI 运行 `cargo fmt --check`。
- [ ] CI 运行 `cargo clippy --workspace --all-targets -- -D warnings`。
- [ ] CI 运行 `cargo test --workspace --all-targets`。
- [ ] CI 运行前端 TypeScript 和生产构建。
- [ ] 引入 Vitest，优先测试摘要规范化、状态转换和请求构造。
- [ ] 为 Simple/Advanced 污点配置增加组件测试。
- [ ] 后续增加关键 Tauri 流程的端到端测试。

## 12. 建议实施顺序

### 迭代 1：正确性基础

- [x] 完成多字节内存污点解析和后端测试。
- [x] 修复关闭构建会话后的后台任务取消。
- [x] 为缓存写入增加临时文件和原子替换。

### 迭代 2：已知摘要匹配

- [x] 实现摘要输入规范化和算法识别。
- [x] 实现字符串候选摘要匹配后端。
- [x] 增加 Tauri command、类型和 Crypto 面板页签。
- [x] 从匹配结果支持跳转、内存查看、XRefs 和数据依赖追踪。

### 迭代 3：简单污点

- [x] 增加 Simple/Advanced 两级模式。
- [x] 增加寄存器、内存、字符串和摘要结果的上下文入口。
- [x] 增加 Summary、Key Steps 和 All Instructions 结果视图。

### 迭代 4：摘要内存定位

- [ ] 实现通用内存字节模式搜索。
- [x] 从摘要输出自动运行多字节反向污点。
- [ ] 提取、评分并验证可能的输入候选（已完成严格摘要候选/输出缓冲区评分，尚缺候选到输出的直接生产关系验证）。

### 迭代 5：分析质量

- [ ] 函数级密码算法识别和可解释置信度。
- [x] 正向污点核心与 MCP 分析记录。
- [x] Trace Diff 和分析报告。

## 13. 首轮预计涉及文件

后端：

- `crates/trace-core/src/engine/slice.rs`
- `crates/trace-core/src/query/slice.rs`
- `crates/trace-core/src/query/hash_match.rs`（新增）
- `crates/trace-core/src/query/mod.rs`
- `crates/trace-core/src/api_types.rs`
- `crates/trace-core/src/session.rs`
- `crates/trace-core/src/cache.rs`
- `src-tauri/src/commands/mod.rs`
- `src-tauri/src/main.rs`

前端：

- `src-web/src/components/TaintConfigDialog.tsx`
- `src-web/src/components/CryptoPanel.tsx`
- `src-web/src/components/StringsPanel.tsx`
- `src-web/src/hooks/useSliceState.ts`
- `src-web/src/types/trace.ts`
- `src-web/src/App.tsx`

测试：

- `crates/trace-core/src/engine/slice.rs` 或独立 slice integration tests
- `crates/trace-core/src/query/hash_match.rs`
- `crates/trace-mcp/tests/integration_test.rs`
- 前端摘要规范化和污点请求构造测试（引入 Vitest 后）

## 14. 完成定义

一个任务只有在满足以下条件后才能勾选完成：

- [ ] 行为有明确的输入、输出和错误语义。
- [ ] Rust 核心逻辑有单元或集成测试。
- [ ] 用户可见流程包含加载、成功、空结果、部分结果和失败状态。
- [ ] 多会话之间状态隔离。
- [ ] 大 Trace 场景没有明显的全量复制或无界内存增长。
- [ ] 缓存 schema 变化有版本处理。
- [x] `cargo test --workspace --all-targets` 通过。
- [x] `npm run build` 通过。
- [ ] 工作区没有无关文件和生成物。

## 15. 决策记录

- 2026-07-16：优先修复多字节内存污点，再实现摘要匹配。
- 2026-07-16：摘要匹配 MVP 先覆盖字符串候选，内存模式搜索放在下一阶段。
- 2026-07-16：污点分析采用 Simple/Advanced 渐进式交互，保留现有高级能力。
- 2026-07-16：严格字节匹配为默认行为，所有会改变输入字节的变换必须显式选择。
- 2026-07-16：完成首轮多字节内存污点、字符串候选摘要匹配、Crypto UI 和 Simple 污点交互；内存摘要定位与 Summary/Key Steps 留到后续迭代。
- 2026-07-16：污点结果增加 Summary、Key Steps 和 All Instructions；Summary 的函数、关键字符串和输入/输出推断仍需后端摘要模型支持。
- 2026-07-16：字符串列表增加 Trace Creation 上下文入口，停靠和浮动窗口统一自动生成多字节内存污点源。
- 2026-07-16：Memory 十六进制视图增加 Trace Memory Value，当前访问范围优先，普通浏览范围限制为最多 4096 字节。
- 2026-07-16：统一污点错误解释层，覆盖配置对话框、摘要、字符串和内存快捷入口，并在 Taint State 保留技术细节。
- 2026-07-16：Known Digest 扩展 CRC32、SHA-384 和 SHA-512；CRC32 按常见大端 8 位十六进制文本匹配。
- 2026-07-16：实现二进制摘要内存定位核心与 Tauri command，按时间重放写入并支持多条 STORE 拼接；前端结果视图留到下一步接入。
- 2026-07-16：Known Digest 接入 Binary memory writes，可查看地址和写入序号，并从完整摘要缓冲区执行 Trace Output。
- 2026-07-16：依赖节点补充函数、模块和内存访问元数据；Summary 关联 String Index 展示关键字符串和输入/输出地址。
- 2026-07-16：SessionHandle 增加 closed 与生命周期锁；关闭会话会广播取消并等待提交临界区退出，保证 close 返回后不再写回状态或缓存。
- 2026-07-16：build_cancel 接入单线程扫描循环、Rayon chunk 扫描和 merge，关闭大 Trace 后后台 CPU 工作可提前结束。
- 2026-07-16：所有缓存写入统一使用同目录 UUID 临时文件，文件 sync 后原子替换；Unix 额外同步目录，Windows 使用 write-through 替换。
- 2026-07-16：SectionReader 改为可失败 typed slice，校验表大小、边界、溢出、对齐和 CSR 结构；损坏 section cache 在装载时删除并回退重建。
- 2026-07-17：MCP 增加 `analyze_known_digest`，默认同时搜索字符串和二进制内存，并对少量命中自动运行完整字节范围的反向数据流分析。
- 2026-07-17：依赖 Summary/Key Steps 下沉到 `trace-core` 的结构化摘要模型，MCP 返回明确证据与限制，不把候选摘要匹配描述为暴力逆向结果。
- 2026-07-17：TraceEngine 增加按 Session 隔离的分析结果存储；摘要和 Crypto Flow 调查返回 `analysis_id`，关闭 Session 时同步清理。
- 2026-07-17：MCP 增加 `list_analyses`、`get_analysis`、`compare_analyses` 和 `delete_analysis`，比较函数、模块、摘要、字符串、内存地址、操作及警告证据。
- 2026-07-18：候选结果增加验证范围、评分因子、扣分项和 verified/related/uncertain 分级，避免把“摘要字节一致”扩大解释为“已证明生产关系”。
- 2026-07-18：Trace Diff 采用执行 Profile 比较，不按行号强行对齐；当前比较函数、分支、指令和内存访问站点，寄存器值差异留待函数输入/输出快照阶段。
- 2026-07-18：自动调查采用结构化请求选择确定性阶段，自然语言 objective 只用于记录和 AI 解释，不在 Rust 内伪造语言推理。
